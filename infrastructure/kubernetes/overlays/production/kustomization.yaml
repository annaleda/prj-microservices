# Placeholder: nessuna differenza da "base" per ora. Qui andranno le
# patch specifiche di produzione (immagini da registry, secret esterni
# via vault, hostname del Gateway, HPA, risorse dimensionate) quando
# l'ambiente esistera'.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
